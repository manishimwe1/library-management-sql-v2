// API Base URL configuration
// For production (Render), use relative paths since frontend and backend are on same domain
// For local development, you can uncomment the localhost line

export const API_BASE = '/api';

// For local development, uncomment this line:
// export const API_BASE = 'http://localhost:3000/api';
